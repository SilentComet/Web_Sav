import { PrismaClient, ServerType, ServerStatus } from '@prisma/client';

console.log('Prisma Client:', PrismaClient);
console.log('ServerType:', ServerType);
console.log('ServerStatus:', ServerStatus);
